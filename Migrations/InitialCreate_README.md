# Run these commands in Package Manager Console (Visual Studio) or terminal:

dotnet ef migrations add InitialCreate
dotnet ef database update

Or in Package Manager Console:
Add-Migration InitialCreate
Update-Database
